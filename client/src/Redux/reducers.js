const User=(state={items:[{code:null,num:null}],info:{}},action)=>{
    switch (action.type) {
        case 'ADD-Item':
            
            break;
    
        default:
            break;
    }
}